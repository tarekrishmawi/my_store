{
  "name": "my-store-full-stack",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "start": "cd frontend && ng serve",
    "start:backend": "cd backend && npm install && npm run docker:up"
  }
}
